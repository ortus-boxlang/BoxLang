/**
 * Compose entry handler. index() returns the native UI tree for the screen.
 * The Kotlin host re-invokes this on recomposition.
 */
class {

	function index() {
		return new NativeScreen().build();
	}

}
