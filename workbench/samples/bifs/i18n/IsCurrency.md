### Check if a value is a valid currency

```java
writeOutput( isCurrency( "$100.00" ) );

```

Result: true

### Returns false for non-currency values

```java
writeOutput( isCurrency( "not a currency" ) );

```

Result: false
