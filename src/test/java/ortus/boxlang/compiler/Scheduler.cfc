component {

	Scheduler function startup(){
        variables.tasks = [];
        variables.tasks.each( function( taskName, taskRecord ){
            if ( true ) {

            } else {

            }

            try {

            } catch ( any e ) {

            }
        } );
	}

}
