import "src.test.java.TestCases.phase3.Stack";

component {
    function init() {
        stack = new Stack();
    }

    function doSomething() {
        if (!stack.empty() ) {
            // do stuff
        }
    }
}