class {
	function init() {
		/* This is a very long multi-line comment that should be wrapped because it exceeds the maximum line length configured in the formatter settings */
		var x = 1;
		return this;
	}
}