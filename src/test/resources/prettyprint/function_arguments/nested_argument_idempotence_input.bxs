result = encodeForHTMLAttribute( absoluteUrl( production.logoUrl,
	platformHome ) );

return rollupData.keyArray().map( ( id ) => rollupData[ id ] ).sort( ( a, b ) => compareNoCase( a.label,
	b.label ) );
