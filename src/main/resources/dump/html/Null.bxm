<!--- Struct template --->
<bx:output>
	<div class="bx-dump" title="#encodeForHTML( posInCode )#">
		<span class="bx-dwSv">
			<span class="bx-dhSv">
				<bx:if label.len() >#label#: </bx:if>
			</span>
			<span>
				<em>&lt;null&gt;</em>
			</span>
		</span>
	</div>
</bx:output>
