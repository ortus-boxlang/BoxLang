<!--- Struct template --->
<bx:output>
	<div class="bx-dump" title="#encodeForHTML( posInCode )#">
		<span class="bx-dwSv">
			<span class="bx-dhSv">
				<bx:if label.len() >#label# - </bx:if>
				NULL:
			</span>
			<span>&lt;null&gt;</span>
		</span>
	</div>
</bx:output>
