<bx:output>
	<div class="bx-dump" title="#encodeForHTML( posInCode )#">
		<span class="bx-dwSv">
			<span class="bx-dhSv" title="length: #var.len()#">
				<bx:if label.len() >#label# - </bx:if>
				String:
			</span>
			<span>#encodeForHTML( var )#</span>
		</span>
	</div>
</bx:output>
